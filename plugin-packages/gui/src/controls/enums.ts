export enum HorizontalAlignment {
  Left,
  Center,
  Right,
  Fill,
}

export enum VerticalAlignment {
  Top,
  Center,
  Bottom,
  Fill,
}

export enum AutowrapMode {
  Off,
  Arbitrary,
  Word,
  WordSmart,
}

export enum TextOverflow {
  Visible,
  Clip,
  Ellipsis,
}

export enum TextureExpandMode {
  KeepSize,
  IgnoreSize,
  FitWidth,
  FitWidthProportional,
  FitHeight,
  FitHeightProportional,
}

export enum TextureStretchMode {
  Scale,
  Tile,
  Keep,
  KeepCentered,
  KeepAspect,
  KeepAspectCentered,
  KeepAspectCovered,
}

export enum AxisStretchMode {
  Stretch,
  Tile,
  TileFit,
}

export enum Side {
  Left,
  Top,
  Right,
  Bottom,
}

export enum ButtonDrawMode {
  Normal,
  Pressed,
  Hover,
  Disabled,
  HoverPressed,
}

export enum ButtonActionMode {
  Press,
  Release,
}

export enum ProgressFillMode {
  BeginToEnd,
  EndToBegin,
  TopToBottom,
  BottomToTop,
}

export enum LayoutOrientation {
  Horizontal,
  Vertical,
}

export enum LayoutAlignment {
  Begin,
  Center,
  End,
}

export enum AspectRatioStretchMode {
  WidthControlsHeight,
  HeightControlsWidth,
  Fit,
  Cover,
}

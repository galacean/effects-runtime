export enum Orientation {
  Horizontal,
  Vertical,
}

export enum LayoutAlignment {
  Begin,
  Center,
  End,
}

export enum AspectRatioStretchMode {
  WidthControlsHeight,
  HeightControlsWidth,
  Fit,
  Cover,
}
